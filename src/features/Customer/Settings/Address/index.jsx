import { VStack } from '@chakra-ui/react';
import AddAddress from './AddAddress';

const Address = () => {
  return (
    <VStack w='full'>
      <AddAddress />
    </VStack>
  );
};

export default Address;
